import { useEffect, useState } from 'react';
import { Grid2X2, List, Search, X } from 'lucide-react';
import { PageHeader } from '../../components/ui/PageHeader.jsx';
import { api } from '../../services/api.js';

const statuses = { DRAFT: 'Draft', CONFIRMED: 'Confirmed', REVISED: 'Revised', CANCELLED: 'Cancelled' };
const money = value => `₹${Number(value || 0).toLocaleString('en-IN', { maximumFractionDigits: 2 })}`;

export function BudgetReportPage() {
  const [budgets, setBudgets] = useState([]); const [view, setView] = useState('list'); const [search, setSearch] = useState(''); const [status, setStatus] = useState('ALL'); const [loading, setLoading] = useState(true); const [error, setError] = useState('');
  const load = async () => { setLoading(true); try { const response = await api.get('/budgets', { params: { search, status } }); setBudgets(response.data.data); } catch (requestError) { setError(requestError.response?.data?.message || 'Unable to load budget report.'); } finally { setLoading(false); } };
  useEffect(() => { const timer = setTimeout(load, 200); return () => clearTimeout(timer); }, [search, status]);
  return <><PageHeader eyebrow="REPORTS / BUDGET" title="Budget report" description="Compare planned amounts with achieved income or expenses." action={<div className="view-toggle report-view-toggle"><button className={view === 'list' ? 'selected' : ''} onClick={() => setView('list')} title="List view"><List size={16} /></button><button className={view === 'kanban' ? 'selected' : ''} onClick={() => setView('kanban')} title="Kanban view"><Grid2X2 size={16} /></button></div>} />{error && <div className="auth-alert error">{error}</div>}<section className="surface users-card"><div className="admin-toolbar"><label className="table-search"><Search size={15} /><input value={search} onChange={event => setSearch(event.target.value)} placeholder="Search budgets" /></label><select value={status} onChange={event => setStatus(event.target.value)}><option value="ALL">All stages</option><option value="DRAFT">Draft</option><option value="CONFIRMED">Confirmed</option><option value="REVISED">Revised</option><option value="CANCELLED">Cancelled</option></select></div>{loading ? <div className="empty-table">Loading budget report...</div> : !budgets.length ? <div className="empty-table">No budgets found.</div> : view === 'list' ? <div className="table-wrap"><table><thead><tr><th>BUDGET</th><th>PERIOD</th><th>ANALYTIC</th><th>PLANNED</th><th>ACHIEVED</th><th>ACHIEVED %</th><th>TO ACHIEVE</th><th>STATUS</th></tr></thead><tbody>{budgets.map(budget => <tr key={budget.id}><td><b>{budget.name}</b><small className="table-subtitle">{budget.responsiblePerson || 'Unassigned'}</small></td><td>{new Date(budget.periodStart).toLocaleDateString('en-IN')} - {new Date(budget.periodEnd).toLocaleDateString('en-IN')}</td><td>{budget.analyticAccount.name}</td><td>{money(budget.amount)}</td><td>{money(budget.achievedAmount)}</td><td>{budget.achievedPercent.toFixed(1)}%</td><td>{money(budget.amountToAchieve)}</td><td><span className={`status-pill ${budget.status === 'CONFIRMED' ? 'active' : budget.status === 'CANCELLED' ? 'inactive' : ''}`}><i />{statuses[budget.status]}</span></td></tr>)}</tbody></table></div> : <div className="budget-kanban">{budgets.map(budget => <article className="budget-card" key={budget.id}><div className="budget-card-top"><span className={`status-pill ${budget.status === 'CONFIRMED' ? 'active' : budget.status === 'CANCELLED' ? 'inactive' : ''}`}><i />{statuses[budget.status]}</span><span>{budget.analyticAccount.type === 'INCOME' ? 'Income' : 'Expenses'}</span></div><h3>{budget.name}</h3><p>{budget.analyticAccount.name}</p><small>{new Date(budget.periodStart).toLocaleDateString('en-IN')} - {new Date(budget.periodEnd).toLocaleDateString('en-IN')}</small><div className="budget-progress"><span style={{ width: `${Math.min(100, budget.achievedPercent)}%` }} /></div><div className="budget-card-metrics"><span><small>Achieved</small><b>{money(budget.achievedAmount)}</b></span><span><small>Planned</small><b>{money(budget.amount)}</b></span></div></article>)}</div>}</section></>;
}
